class row:
  def __init__(self, capacity):
    self.capacity = capacity
    self.data = [0] * capacity
    self.servers = []

def Print(self):
  for i in range(capacity):
    print(self.data[i])

def addServer(self,server):
  self.servers.append(server)

def changeStatus(self, index, server)
  self.data[îndex] = server


